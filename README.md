# aws-s3-multipart-upload-service
Spring Boot 3 + Java 21 microservice (OAuth2 Resource Server) that authorizes users, creates S3 multipart uploads, returns pre-signed part URLs, and completes/aborts uploads. Persists upload metadata in PostgreSQL via JPA/Hibernate + Flyway
